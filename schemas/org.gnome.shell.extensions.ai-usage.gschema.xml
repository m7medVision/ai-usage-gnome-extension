<?xml version="1.0" encoding="UTF-8"?>
<schemalist>
  <schema id="org.gnome.shell.extensions.ai-usage"
          path="/org/gnome/shell/extensions/ai-usage/">

    <!-- ====== General UI settings ====== -->
    <!-- Provider accounts/credentials are stored in
         ${XDG_DATA_HOME}/.ai-usage-ext/config.json, not here. -->

    <key name="refresh-interval" type="i">
      <default>300</default>
      <range min="30" max="3600"/>
      <summary>Refresh Interval (seconds)</summary>
      <description>How often to refresh usage data from all enabled accounts (30-3600 seconds).</description>
    </key>

    <key name="show-logos" type="b">
      <default>true</default>
      <summary>Show provider logos</summary>
      <description>Whether to show provider logos on the provider tabs in the popup menu.</description>
    </key>

    <key name="display-mode" type="s">
      <choices><choice value='used'/><choice value='remaining'/></choices>
      <default>'used'</default>
      <summary>Display mode</summary>
      <description>Whether the indicator and progress bars reflect "used" or "remaining" quota.</description>
    </key>

    <key name="high-usage-threshold" type="i">
      <default>80</default>
      <range min="50" max="100"/>
      <summary>High usage warning threshold (%)</summary>
      <description>Usage percentage above which the indicator turns orange.</description>
    </key>

    <key name="critical-usage-threshold" type="i">
      <default>95</default>
      <range min="60" max="100"/>
      <summary>Critical usage threshold (%)</summary>
      <description>Usage percentage above which the indicator turns red.</description>
    </key>

    <key name="usage-alerts-enabled" type="b">
      <default>true</default>
      <summary>Enable usage alerts</summary>
      <description>Whether a desktop alert is shown when a quota window reaches the alert threshold.</description>
    </key>

    <key name="usage-alert-threshold" type="i">
      <default>90</default>
      <range min="50" max="100"/>
      <summary>Usage alert threshold (%)</summary>
      <description>Usage percentage at which a desktop alert is shown.</description>
    </key>

  </schema>
</schemalist>
